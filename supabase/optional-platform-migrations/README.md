# Optional platform migrations

These immutable platform-source migrations are retained for checksum and provenance checks, but they are not part of the Official Assembly production migration lineage.

Do not move them into `supabase/migrations` or apply them to production without a separate feature approval and migration-history review. The deployable directory intentionally contains only the production baseline followed by the approved newsletter migrations.
